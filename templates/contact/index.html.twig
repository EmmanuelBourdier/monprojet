{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<main class="w-50 mx-auto my-5">
    <h1 class="my-4">Contactez-nous</h1>
    {{ form_start(form) }}
    {{ form(form) }}
    <div class="form-group" id="btn-contact">
    <button type="submit" class="btn btn-primary">Envoyer</button>
    </div>
    {{ form_end(form) }}
  </main>

{% endblock %}
