<section>
  <figure class="text-center container py-2 bg-grey mt-2">
    <div class="justify-content-center mb-2"><h1 class="prix-title">Avis de nos clients</h1></div>
    <div class="d-flex">
    {% for commentaire in commentaires %}
      <div class="list-group my-2 mx-2 comment">
  <a href="{{path('app_commentaire_liste')}}" class="list-group-item list-group-item-action flex-column align-items-start">
    <div class="d-flex justify-content-between comment-head">
      <h6 class="mb-1">{{ commentaire.nom }} {{ commentaire.prenom }}</h6>
      <small class="text-muted">{{ commentaire.createAt|date('d-m-Y') }}</small>
    </div>
    <div class="comment-body">
    <p class="mb-1  text-truncate-2 text-start mt-2" >{{ commentaire.message }}</p>
    </div>
  </a>
</div>
    {% endfor %}
  </div>  
  </figure>
  </section>